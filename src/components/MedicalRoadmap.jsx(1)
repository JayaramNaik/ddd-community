// ─────────────────────────────────────────────────────────────
//  src/components/MedicalRoadmap.jsx
//  Full Medical Career Roadmap for 10th standard students
// ─────────────────────────────────────────────────────────────

import { useState } from "react";
import { FadeIn } from "../hooks/useInView.jsx";

const MEDICAL_FIELDS = [
  {
    id: "mbbs", icon: "🩺", title: "MBBS", color: "#ef4444",
    desc: "The most prestigious medical degree. Become a doctor — diagnose, treat, and heal patients.",
    duration: "5.5 years + internship",
    eligibility: "PCB in 12th + NEET UG",
    skills: ["Biology mastery", "Patient communication", "Clinical reasoning", "Anatomy & Physiology"],
    scope: ["General Physician", "Surgeon", "Specialist Doctor", "Medical Researcher"],
    colleges: ["AIIMS Delhi", "JIPMER Puducherry", "Kasturba Medical College", "St. John's Medical Bengaluru"],
    exams: ["NEET UG"],
    specializations: ["Cardiology", "Neurology", "Pediatrics", "Psychiatry", "Oncology", "Orthopedics"],
  },
  {
    id: "bds", icon: "🦷", title: "BDS (Dental)", color: "#f59e0b",
    desc: "Dental surgery — treat teeth, gums, and oral health. High demand, good scope for private practice.",
    duration: "5 years",
    eligibility: "PCB in 12th + NEET UG",
    skills: ["Fine motor skills", "Anatomy", "Patient care", "Precision work"],
    scope: ["Dentist", "Oral Surgeon", "Orthodontist", "Dental Researcher"],
    colleges: ["Manipal College of Dental Sciences", "KLE VK Institute", "SDM Dharwad"],
    exams: ["NEET UG"],
    specializations: ["Orthodontics", "Oral Surgery", "Periodontology", "Endodontics"],
  },
  {
    id: "ayurveda", icon: "🌿", title: "BAMS (Ayurveda)", color: "#34d399",
    desc: "Ancient Indian medicine system. Growing globally with focus on natural healing and wellness.",
    duration: "5.5 years",
    eligibility: "PCB in 12th + NEET UG",
    skills: ["Herbal medicine", "Holistic diagnosis", "Yoga & wellness", "Sanskrit basics"],
    scope: ["Ayurvedic Doctor", "Wellness Consultant", "Panchakarma Specialist", "Researcher"],
    colleges: ["SDM College of Ayurveda", "KLE Ayurveda College", "RGUHS affiliated colleges"],
    exams: ["NEET UG"],
    specializations: ["Panchakarma", "Kayachikitsa", "Shalya Tantra", "Rasayana"],
  },
  {
    id: "nursing", icon: "💉", title: "Nursing (B.Sc)", color: "#38bdf8",
    desc: "The backbone of healthcare. Nurses are critical in hospitals, ICUs, and community health.",
    duration: "4 years",
    eligibility: "PCB in 12th, some require NEET",
    skills: ["Patient care", "Clinical procedures", "Emergency response", "Empathy"],
    scope: ["Staff Nurse", "ICU Nurse", "Community Health Worker", "Nurse Educator"],
    colleges: ["NIMHANS Bengaluru", "KMC Manipal", "St. John's Bengaluru", "Govt nursing colleges"],
    exams: ["University entrance", "State nursing CET"],
    specializations: ["Critical Care", "Pediatric Nursing", "Oncology Nursing", "Mental Health"],
  },
  {
    id: "pharmacy", icon: "💊", title: "Pharmacy (B.Pharm)", color: "#818cf8",
    desc: "Study medicines — how they work, how they're made. Critical in hospitals, pharma companies, and research.",
    duration: "4 years",
    eligibility: "PCB or PCM in 12th",
    skills: ["Chemistry", "Pharmacology", "Drug analysis", "Research methodology"],
    scope: ["Clinical Pharmacist", "Drug Inspector", "Pharma R&D", "Medical Representative"],
    colleges: ["JSS College of Pharmacy Mysuru", "NGSM Mangaluru", "KLE College of Pharmacy"],
    exams: ["GPAT (PG)", "State pharmacy CET"],
    specializations: ["Clinical Pharmacy", "Pharmaceutical Chemistry", "Pharmacognosy"],
  },
  {
    id: "physiotherapy", icon: "🦴", title: "Physiotherapy (BPT)", color: "#f472b6",
    desc: "Restore movement and function. Help patients recover from injuries, surgeries, and disabilities.",
    duration: "4.5 years",
    eligibility: "PCB in 12th",
    skills: ["Anatomy", "Exercise therapy", "Rehabilitation", "Manual therapy"],
    scope: ["Physiotherapist", "Sports Rehab Specialist", "Neuro Physiotherapist", "Pain Clinic"],
    colleges: ["Manipal College of Health Professions", "KMC Mangaluru", "KIMS Hubli"],
    exams: ["University entrance", "State CET"],
    specializations: ["Sports Physiotherapy", "Neuro Rehab", "Pediatric PT", "Cardiopulmonary PT"],
  },
  {
    id: "biotech", icon: "🧬", title: "Medical Biotechnology", color: "#a855f7",
    desc: "Merge biology with technology — develop vaccines, diagnostics, and medical devices.",
    duration: "4 years (B.Tech Biotech)",
    eligibility: "PCB/PCM in 12th + JEE/CET",
    skills: ["Molecular Biology", "Genetics", "Lab techniques", "Bioinformatics"],
    scope: ["Research Scientist", "Biomedical Engineer", "Clinical Research Associate", "Vaccine Developer"],
    colleges: ["IIT Delhi", "VIT Vellore", "Manipal", "Amrita"],
    exams: ["JEE Main", "KCET", "CUET"],
    specializations: ["Genomics", "Drug Discovery", "Medical Devices", "Diagnostics"],
  },
  {
    id: "research", icon: "🔬", title: "Medical Research (BSc)", color: "#10b981",
    desc: "Work in labs discovering cures, understanding diseases, and advancing medical science.",
    duration: "3 years BSc + MSc/PhD",
    eligibility: "PCB in 12th",
    skills: ["Research methodology", "Lab skills", "Statistics", "Scientific writing"],
    scope: ["Research Scientist", "Lab Analyst", "Clinical Researcher", "Academic Professor"],
    colleges: ["IISc Bengaluru", "NIMHANS", "JNCASR", "TIFR Mumbai"],
    exams: ["CUET", "University entrance", "DBT/ICMR fellowships (PG)"],
    specializations: ["Microbiology", "Immunology", "Neuroscience", "Epidemiology"],
  },
];

const ROADMAP_STEPS = [
  {
    phase: "Now (10th)",
    color: "#ef4444",
    icon: "🏫",
    steps: [
      "Focus hard on Science & Biology — score 90%+",
      "Read about human body — start with NCERT Biology",
      "Watch medical documentaries and surgeries (YouTube)",
      "Visit a hospital or clinic — observe, ask questions",
      "Build reading habits — science magazines, health news",
    ],
  },
  {
    phase: "PUC / 11th–12th",
    color: "#f59e0b",
    icon: "📖",
    steps: [
      "Choose PCB (Physics, Chemistry, Biology) — mandatory for NEET",
      "Start NEET coaching from 11th itself — don't delay",
      "NCERT Biology is your bible — read every line",
      "Practice diagrams — heart, cell, nervous system etc.",
      "Take chapter-wise tests every week without fail",
    ],
  },
  {
    phase: "NEET Preparation Year",
    color: "#818cf8",
    icon: "📝",
    steps: [
      "Appear for NEET UG — held once a year (May)",
      "Target 600+ for government medical college",
      "550+ for good private college with scholarship",
      "Prepare for state counselling (Karnataka: KEA)",
      "Don't give up if first attempt isn't perfect — retake",
    ],
  },
  {
    phase: "Medical Degree (5.5 years)",
    color: "#34d399",
    icon: "🎓",
    steps: [
      "MBBS has 4.5 years of academics + 1 year internship",
      "Attend all clinical postings — hospitals are your classroom",
      "Build good relationships with professors and seniors",
      "Prepare for PG entrance (NEET PG) from 3rd year",
      "Do elective rotations in departments you love",
    ],
  },
  {
    phase: "Postgraduate & Beyond",
    color: "#a855f7",
    icon: "🚀",
    steps: [
      "NEET PG for MD/MS specialization (2–3 years)",
      "SuperSpecialty (DM/MCh) after MD/MS",
      "Fellowship programs in India or abroad",
      "USMLE / PLAB for USA / UK practice",
      "Research, teaching, or private practice",
    ],
  },
];

const NEET_TIMELINE = [
  { month: "June–July (11th start)", task: "Start NCERT — Biology, Physics, Chemistry. Join coaching.", icon: "📚" },
  { month: "Aug–Nov (11th)", task: "Complete 11th syllabus. Daily Biology diagrams. Weekly tests.", icon: "✏️" },
  { month: "Dec–March (11th end)", task: "Revise 11th fully. Start 12th topics in parallel.", icon: "🔄" },
  { month: "April–Aug (12th)", task: "Complete 12th syllabus. Solve NEET past papers topic-wise.", icon: "📝" },
  { month: "Sept–Dec (12th)", task: "Full syllabus revision. Mock tests 3x per week.", icon: "🎯" },
  { month: "Jan–March", task: "Board exams + NEET revision together. Don't panic.", icon: "⚡" },
  { month: "April–May", task: "Final sprint. Past 10 years NEET papers daily.", icon: "🔥" },
  { month: "May (NEET Day)", task: "Appear for NEET UG. Stay calm. You've prepared well.", icon: "🏆" },
];

const RESOURCES = [
  { category: "📗 Books", items: ["NCERT Biology (11th & 12th) — most important", "DC Pandey — Physics", "OP Tandon — Chemistry", "Trueman's Biology", "MTG Fingertips Biology"] },
  { category: "🖥️ Online Platforms", items: ["Unacademy NEET", "Physics Wallah NEET batch", "ALLEN Online", "Aakash Digital", "NEET Testbook"] },
  { category: "📱 Apps & Tools", items: ["Doubtnut (doubt solving)", "NEET Prep App", "Human Anatomy Atlas", "Osmosis (video learning)", "Anki (flashcards)"] },
  { category: "🏛️ YouTube Channels", items: ["Physics Wallah (Bio, Chem)", "Vedantu NEET", "Dr. Najeeb Lectures (Anatomy)", "Ninja Nerd (Physiology)", "Rajat Arora (Biology)"] },
];

const STORIES = [
  { name: "Dr. Devi Shetty", role: "Founder, Narayana Health", quote: "Compassion is the most important quality a doctor can have. Knowledge can be learned, but empathy must come from within.", field: "Cardiac Surgery" },
  { name: "Dr. Tessy Thomas", role: "Missile Scientist, DRDO", quote: "There is no shortcut to success. Hard work, dedication and perseverance are the only keys.", field: "Aerospace + Science" },
  { name: "Dr. V. Mohan", role: "Diabetes Specialist, Chennai", quote: "Prevention is always better than cure. Educate your patients, not just treat them.", field: "Diabetology" },
];

const MENTAL_HEALTH_TIPS = [
  { icon: "😴", tip: "Sleep 7–8 hours", detail: "Memory consolidation happens during sleep. Never sacrifice sleep for extra study." },
  { icon: "🏃", tip: "Exercise daily", detail: "Even 30 minutes of walking improves focus and reduces anxiety significantly." },
  { icon: "🧘", tip: "Meditate / breathe", detail: "5 minutes of deep breathing before study sessions boosts concentration." },
  { icon: "👨‍👩‍👧", tip: "Talk to family", detail: "Share your stress. Don't bottle it up. Your family's support matters more than you know." },
  { icon: "📵", tip: "Social media breaks", detail: "Avoid comparing your progress with others. Everyone's journey is different." },
  { icon: "🎯", tip: "Celebrate small wins", detail: "Finished a chapter? That's worth celebrating. Progress is progress." },
];

export default function MedicalRoadmap({ dark }) {
  const [activeField, setActiveField] = useState(null);
  const [activeTab, setActiveTab] = useState("roadmap");

  const tabs = [
    { key: "roadmap",   label: "📍 Roadmap" },
    { key: "fields",    label: "🩺 Medical Fields" },
    { key: "neet",      label: "📝 NEET Prep" },
    { key: "resources", label: "📚 Resources" },
    { key: "wellbeing", label: "🧘 Wellbeing" },
    { key: "stories",   label: "🌟 Inspiration" },
  ];

  return (
    <section id="medical" style={{ minHeight: "100vh", background: dark ? "#040b1c" : "#f8fafc", padding: "80px 2rem 120px" }}>
      <div style={{ maxWidth: 1100, margin: "0 auto" }}>

        {/* ── Hero ── */}
        <FadeIn>
          <div style={{ textAlign: "center", marginBottom: 64 }}>
            <div style={{
              display: "inline-block",
              background: "linear-gradient(135deg,#ef444422,#f472b822)",
              border: "1px solid #ef444433",
              borderRadius: 40,
              padding: "6px 20px",
              fontSize: 11,
              fontWeight: 700,
              color: "#ef4444",
              letterSpacing: "0.14em",
              textTransform: "uppercase",
              fontFamily: "'Syne',sans-serif",
              marginBottom: 20,
            }}>
              🩺 Career Roadmap
            </div>
            <h1 style={{
              fontSize: "clamp(2.2rem,5vw,3.8rem)",
              fontFamily: "'Playfair Display',serif",
              fontWeight: 900,
              color: dark ? "#f1f5f9" : "#0f172a",
              margin: "0 0 20px",
              lineHeight: 1.15,
            }}>
              Future{" "}
              <span style={{ background: "linear-gradient(135deg,#ef4444,#f472b8)", WebkitBackgroundClip: "text", WebkitTextFillColor: "transparent" }}>
                Doctor
              </span>{" "}
              Roadmap
            </h1>
            <p style={{
              fontSize: "clamp(1rem,2vw,1.15rem)",
              color: dark ? "#64748b" : "#64748b",
              fontFamily: "'Lora',serif",
              maxWidth: 560,
              margin: "0 auto 32px",
              lineHeight: 1.8,
            }}>
              You're in 10th standard and you want to save lives. Here's your complete guide — from today until you wear that white coat.
            </p>

            {/* Stats */}
            <div style={{ display: "flex", justifyContent: "center", gap: 32, flexWrap: "wrap" }}>
              {[
                { label: "Medical career paths", value: "8+" },
                { label: "NEET seats in India", value: "1L+" },
                { label: "Years to become doctor", value: "5.5–9" },
                { label: "Job security", value: "Very High" },
              ].map(s => (
                <div key={s.label} style={{ textAlign: "center" }}>
                  <div style={{ fontFamily: "'Playfair Display',serif", fontWeight: 900, fontSize: 28, background: "linear-gradient(135deg,#ef4444,#f472b8)", WebkitBackgroundClip: "text", WebkitTextFillColor: "transparent" }}>
                    {s.value}
                  </div>
                  <div style={{ fontFamily: "'Syne',sans-serif", fontSize: 11, color: dark ? "#64748b" : "#94a3b8", textTransform: "uppercase", letterSpacing: "0.08em", fontWeight: 600 }}>
                    {s.label}
                  </div>
                </div>
              ))}
            </div>
          </div>
        </FadeIn>

        {/* ── Why Medical ── */}
        <FadeIn delay={0.1}>
          <div style={{
            background: dark ? "linear-gradient(135deg,rgba(239,68,68,0.07),rgba(244,114,182,0.07))" : "linear-gradient(135deg,rgba(239,68,68,0.05),rgba(244,114,182,0.04))",
            border: `1px solid ${dark ? "rgba(239,68,68,0.15)" : "rgba(239,68,68,0.2)"}`,
            borderRadius: 24,
            padding: "36px",
            marginBottom: 56,
          }}>
            <h2 style={{ fontFamily: "'Playfair Display',serif", fontWeight: 900, fontSize: "clamp(1.4rem,3vw,2rem)", color: dark ? "#f1f5f9" : "#0f172a", margin: "0 0 20px" }}>
              🤔 Why Choose the Medical Field?
            </h2>
            <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(220px,1fr))", gap: 16 }}>
              {[
                { icon: "❤️", title: "Save Lives", desc: "Every single day you go to work, you make a real difference in someone's life." },
                { icon: "🏆", title: "Respect & Trust", desc: "Doctors are among the most trusted and respected professionals in society." },
                { icon: "🌍", title: "Global Demand", desc: "Qualified doctors are needed everywhere — India, USA, UK, Middle East, Australia." },
                { icon: "📈", title: "Career Security", desc: "Medical profession is recession-proof. Illness doesn't take a break." },
                { icon: "🔬", title: "Lifelong Learning", desc: "Medicine is always evolving — you'll never stop growing and learning." },
                { icon: "💼", title: "Multiple Paths", desc: "MBBS, BDS, Ayurveda, Nursing, Pharmacy, Research — many entry points." },
              ].map(r => (
                <div key={r.title} style={{
                  background: dark ? "rgba(255,255,255,0.04)" : "rgba(255,255,255,0.8)",
                  borderRadius: 14,
                  padding: "18px 16px",
                  border: `1px solid ${dark ? "rgba(255,255,255,0.06)" : "rgba(0,0,0,0.05)"}`,
                }}>
                  <div style={{ fontSize: 28, marginBottom: 8 }}>{r.icon}</div>
                  <div style={{ fontFamily: "'Syne',sans-serif", fontWeight: 700, fontSize: 14, color: dark ? "#f1f5f9" : "#0f172a", marginBottom: 4 }}>{r.title}</div>
                  <div style={{ fontFamily: "'Lora',serif", fontSize: 12, color: dark ? "#64748b" : "#64748b", lineHeight: 1.6 }}>{r.desc}</div>
                </div>
              ))}
            </div>
          </div>
        </FadeIn>

        {/* ── Tab Navigation ── */}
        <FadeIn delay={0.15}>
          <div style={{ display: "flex", gap: 8, marginBottom: 40, flexWrap: "wrap" }}>
            {tabs.map(tab => (
              <button
                key={tab.key}
                onClick={() => setActiveTab(tab.key)}
                style={{
                  background: activeTab === tab.key
                    ? "linear-gradient(135deg,#ef4444,#f472b8)"
                    : (dark ? "rgba(255,255,255,0.05)" : "#fff"),
                  border: `1.5px solid ${activeTab === tab.key ? "transparent" : (dark ? "rgba(255,255,255,0.1)" : "rgba(0,0,0,0.08)")}`,
                  borderRadius: 12,
                  padding: "10px 20px",
                  cursor: "pointer",
                  fontFamily: "'Syne',sans-serif",
                  fontWeight: 700,
                  fontSize: 13,
                  color: activeTab === tab.key ? "#fff" : (dark ? "#94a3b8" : "#64748b"),
                  transition: "all 0.25s",
                  whiteSpace: "nowrap",
                }}
              >
                {tab.label}
              </button>
            ))}
          </div>
        </FadeIn>

        {/* ── Roadmap Tab ── */}
        {activeTab === "roadmap" && (
          <FadeIn>
            <div style={{ marginBottom: 48 }}>
              <h2 style={{ fontFamily: "'Playfair Display',serif", fontWeight: 900, fontSize: "clamp(1.4rem,3vw,2rem)", color: dark ? "#f1f5f9" : "#0f172a", margin: "0 0 32px" }}>
                📍 Your Step-by-Step Roadmap
              </h2>
              <div style={{ display: "flex", flexDirection: "column", gap: 0 }}>
                {ROADMAP_STEPS.map((phase, i) => (
                  <div key={phase.phase} style={{ display: "flex", gap: 24, alignItems: "flex-start" }}>
                    <div style={{ display: "flex", flexDirection: "column", alignItems: "center", flexShrink: 0 }}>
                      <div style={{
                        width: 48, height: 48, borderRadius: "50%",
                        background: `linear-gradient(135deg,${phase.color},${phase.color}88)`,
                        display: "flex", alignItems: "center", justifyContent: "center",
                        fontSize: 20, flexShrink: 0,
                        boxShadow: `0 0 20px ${phase.color}44`,
                      }}>
                        {phase.icon}
                      </div>
                      {i < ROADMAP_STEPS.length - 1 && (
                        <div style={{ width: 2, flex: 1, minHeight: 40, background: `linear-gradient(${phase.color},${ROADMAP_STEPS[i+1].color})`, margin: "8px 0", opacity: 0.4 }} />
                      )}
                    </div>
                    <div style={{
                      background: dark ? "rgba(255,255,255,0.03)" : "#fff",
                      border: `1.5px solid ${dark ? "rgba(255,255,255,0.07)" : "rgba(0,0,0,0.07)"}`,
                      borderLeft: `3px solid ${phase.color}`,
                      borderRadius: 16,
                      padding: "20px 24px",
                      marginBottom: 16,
                      flex: 1,
                    }}>
                      <div style={{ fontFamily: "'Syne',sans-serif", fontWeight: 800, fontSize: 16, color: phase.color, marginBottom: 12 }}>
                        {phase.phase}
                      </div>
                      <ul style={{ margin: 0, padding: 0, listStyle: "none", display: "flex", flexDirection: "column", gap: 8 }}>
                        {phase.steps.map((step, j) => (
                          <li key={j} style={{ display: "flex", gap: 10, alignItems: "flex-start" }}>
                            <span style={{ color: phase.color, fontWeight: 700, flexShrink: 0, marginTop: 2 }}>→</span>
                            <span style={{ fontFamily: "'Lora',serif", fontSize: 14, color: dark ? "#cbd5e1" : "#334155", lineHeight: 1.6 }}>{step}</span>
                          </li>
                        ))}
                      </ul>
                    </div>
                  </div>
                ))}
              </div>
            </div>

            {/* Skills */}
            <div style={{
              background: dark ? "rgba(255,255,255,0.03)" : "#fff",
              border: `1.5px solid ${dark ? "rgba(255,255,255,0.08)" : "rgba(0,0,0,0.07)"}`,
              borderRadius: 20,
              padding: "32px",
              marginBottom: 48,
            }}>
              <h3 style={{ fontFamily: "'Playfair Display',serif", fontWeight: 900, fontSize: "clamp(1.2rem,2.5vw,1.6rem)", color: dark ? "#f1f5f9" : "#0f172a", margin: "0 0 24px" }}>
                ⚡ Skills Every Doctor Needs
              </h3>
              <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(200px,1fr))", gap: 12 }}>
                {[
                  { icon: "🧬", skill: "Biology Depth", note: "Cell biology, genetics, human anatomy" },
                  { icon: "❤️", skill: "Empathy", note: "Connect with patients emotionally" },
                  { icon: "🧠", skill: "Critical Thinking", note: "Diagnose from complex symptoms" },
                  { icon: "✋", skill: "Steady Hands", note: "Surgical precision and fine motor skills" },
                  { icon: "📖", skill: "Lifelong Learning", note: "Medicine updates every year" },
                  { icon: "🗣️", skill: "Communication", note: "Explain clearly to patients & families" },
                  { icon: "⏱️", skill: "Decision Under Pressure", note: "Emergency situations need calm clarity" },
                  { icon: "🤝", skill: "Teamwork", note: "Doctors, nurses, paramedics — all one team" },
                ].map(s => (
                  <div key={s.skill} style={{
                    background: dark ? "rgba(239,68,68,0.06)" : "rgba(239,68,68,0.04)",
                    border: "1px solid rgba(239,68,68,0.15)",
                    borderRadius: 12,
                    padding: "14px 16px",
                    display: "flex",
                    gap: 12,
                    alignItems: "flex-start",
                  }}>
                    <span style={{ fontSize: 22, flexShrink: 0 }}>{s.icon}</span>
                    <div>
                      <div style={{ fontFamily: "'Syne',sans-serif", fontWeight: 700, fontSize: 13, color: dark ? "#e2e8f0" : "#1e293b" }}>{s.skill}</div>
                      <div style={{ fontFamily: "'Lora',serif", fontSize: 11, color: dark ? "#64748b" : "#94a3b8", marginTop: 2 }}>{s.note}</div>
                    </div>
                  </div>
                ))}
              </div>
            </div>

            {/* Day in the life */}
            <div style={{
              background: dark ? "rgba(244,114,182,0.06)" : "rgba(244,114,182,0.04)",
              border: "1px solid rgba(244,114,182,0.2)",
              borderRadius: 20,
              padding: "32px",
            }}>
              <h3 style={{ fontFamily: "'Playfair Display',serif", fontWeight: 900, fontSize: "clamp(1.2rem,2.5vw,1.6rem)", color: dark ? "#f1f5f9" : "#0f172a", margin: "0 0 20px" }}>
                ☀️ A Day in the Life of a Doctor
              </h3>
              <div style={{ display: "flex", flexDirection: "column", gap: 10 }}>
                {[
                  { time: "7:00 AM", act: "Morning rounds — visit admitted patients, check progress" },
                  { time: "9:00 AM", act: "OPD begins — diagnose and treat outpatients" },
                  { time: "1:00 PM", act: "Lunch break — brief but necessary" },
                  { time: "2:00 PM", act: "Surgeries or procedures (for surgical specialties)" },
                  { time: "4:00 PM", act: "Review test reports, update treatment plans" },
                  { time: "6:00 PM", act: "Evening rounds — check admitted patients again" },
                  { time: "8:00 PM", act: "Research, medical journal reading, or family time" },
                ].map(item => (
                  <div key={item.time} style={{
                    display: "flex", gap: 16, alignItems: "center",
                    background: dark ? "rgba(255,255,255,0.03)" : "#fff",
                    borderRadius: 10, padding: "12px 16px",
                    border: `1px solid ${dark ? "rgba(255,255,255,0.05)" : "rgba(0,0,0,0.05)"}`,
                  }}>
                    <span style={{ fontFamily: "'Syne',sans-serif", fontWeight: 700, fontSize: 12, color: "#ef4444", minWidth: 60, flexShrink: 0 }}>{item.time}</span>
                    <span style={{ fontFamily: "'Lora',serif", fontSize: 13, color: dark ? "#cbd5e1" : "#334155" }}>{item.act}</span>
                  </div>
                ))}
              </div>
            </div>
          </FadeIn>
        )}

        {/* ── Medical Fields Tab ── */}
        {activeTab === "fields" && (
          <FadeIn>
            <h2 style={{ fontFamily: "'Playfair Display',serif", fontWeight: 900, fontSize: "clamp(1.4rem,3vw,2rem)", color: dark ? "#f1f5f9" : "#0f172a", margin: "0 0 32px" }}>
              🩺 Medical Career Paths — Find Your Calling
            </h2>
            <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill,minmax(300px,1fr))", gap: 20 }}>
              {MEDICAL_FIELDS.map(field => (
                <div
                  key={field.id}
                  onClick={() => setActiveField(activeField?.id === field.id ? null : field)}
                  style={{
                    background: dark ? "rgba(255,255,255,0.03)" : "#fff",
                    border: `1.5px solid ${activeField?.id === field.id ? field.color : (dark ? "rgba(255,255,255,0.08)" : "rgba(0,0,0,0.07)")}`,
                    borderTop: `3px solid ${field.color}`,
                    borderRadius: 18,
                    padding: "24px 20px",
                    cursor: "pointer",
                    transition: "all 0.3s",
                  }}
                  onMouseEnter={e => { e.currentTarget.style.transform = "translateY(-4px)"; e.currentTarget.style.boxShadow = `0 16px 40px ${field.color}22`; }}
                  onMouseLeave={e => { e.currentTarget.style.transform = ""; e.currentTarget.style.boxShadow = ""; }}
                >
                  <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start", marginBottom: 12 }}>
                    <span style={{ fontSize: 36 }}>{field.icon}</span>
                    <span style={{ background: field.color + "18", color: field.color, border: `1px solid ${field.color}33`, fontSize: 11, padding: "3px 10px", borderRadius: 20, fontFamily: "'Syne',sans-serif", fontWeight: 700 }}>
                      {field.duration}
                    </span>
                  </div>
                  <h3 style={{ fontFamily: "'Syne',sans-serif", fontWeight: 800, fontSize: 17, color: dark ? "#f1f5f9" : "#0f172a", margin: "0 0 8px" }}>{field.title}</h3>
                  <p style={{ fontFamily: "'Lora',serif", fontSize: 13, color: dark ? "#64748b" : "#64748b", lineHeight: 1.6, margin: "0 0 16px" }}>{field.desc}</p>

                  {activeField?.id === field.id && (
                    <div style={{ borderTop: `1px solid ${dark ? "rgba(255,255,255,0.08)" : "rgba(0,0,0,0.07)"}`, paddingTop: 16 }}>
                      <div style={{ marginBottom: 12 }}>
                        <div style={{ fontFamily: "'Syne',sans-serif", fontWeight: 700, fontSize: 12, color: field.color, textTransform: "uppercase", letterSpacing: "0.08em", marginBottom: 8 }}>Eligibility</div>
                        <div style={{ fontFamily: "'Lora',serif", fontSize: 12, color: dark ? "#94a3b8" : "#64748b" }}>{field.eligibility}</div>
                      </div>
                      <div style={{ marginBottom: 12 }}>
                        <div style={{ fontFamily: "'Syne',sans-serif", fontWeight: 700, fontSize: 12, color: field.color, textTransform: "uppercase", letterSpacing: "0.08em", marginBottom: 8 }}>Key Skills</div>
                        {field.skills.map(s => (
                          <div key={s} style={{ fontFamily: "'Lora',serif", fontSize: 12, color: dark ? "#94a3b8" : "#64748b", padding: "2px 0" }}>→ {s}</div>
                        ))}
                      </div>
                      <div style={{ marginBottom: 12 }}>
                        <div style={{ fontFamily: "'Syne',sans-serif", fontWeight: 700, fontSize: 12, color: field.color, textTransform: "uppercase", letterSpacing: "0.08em", marginBottom: 8 }}>Career Roles</div>
                        <div style={{ display: "flex", flexWrap: "wrap", gap: 6 }}>
                          {field.scope.map(s => (
                            <span key={s} style={{ background: field.color + "15", color: field.color, border: `1px solid ${field.color}25`, padding: "3px 10px", borderRadius: 20, fontSize: 11, fontFamily: "'Syne',sans-serif", fontWeight: 600 }}>{s}</span>
                          ))}
                        </div>
                      </div>
                      <div style={{ marginBottom: 12 }}>
                        <div style={{ fontFamily: "'Syne',sans-serif", fontWeight: 700, fontSize: 12, color: field.color, textTransform: "uppercase", letterSpacing: "0.08em", marginBottom: 8 }}>Specializations</div>
                        <div style={{ display: "flex", flexWrap: "wrap", gap: 6 }}>
                          {field.specializations.map(s => (
                            <span key={s} style={{ background: dark ? "rgba(255,255,255,0.07)" : "#f1f5f9", color: dark ? "#cbd5e1" : "#475569", padding: "3px 10px", borderRadius: 20, fontSize: 11, fontFamily: "'Syne',sans-serif", fontWeight: 600 }}>{s}</span>
                          ))}
                        </div>
                      </div>
                      <div style={{ marginBottom: 12 }}>
                        <div style={{ fontFamily: "'Syne',sans-serif", fontWeight: 700, fontSize: 12, color: field.color, textTransform: "uppercase", letterSpacing: "0.08em", marginBottom: 8 }}>Top Colleges</div>
                        {field.colleges.map(c => (
                          <div key={c} style={{ fontFamily: "'Lora',serif", fontSize: 12, color: dark ? "#94a3b8" : "#64748b", padding: "2px 0" }}>🏥 {c}</div>
                        ))}
                      </div>
                      <div>
                        <div style={{ fontFamily: "'Syne',sans-serif", fontWeight: 700, fontSize: 12, color: field.color, textTransform: "uppercase", letterSpacing: "0.08em", marginBottom: 8 }}>Key Exams</div>
                        <div style={{ display: "flex", flexWrap: "wrap", gap: 6 }}>
                          {field.exams.map(e => (
                            <span key={e} style={{ background: dark ? "rgba(255,255,255,0.07)" : "#f1f5f9", color: dark ? "#cbd5e1" : "#475569", padding: "3px 10px", borderRadius: 20, fontSize: 11, fontFamily: "'Syne',sans-serif", fontWeight: 600 }}>{e}</span>
                          ))}
                        </div>
                      </div>
                    </div>
                  )}

                  <div style={{ marginTop: 12, color: field.color, fontSize: 12, fontFamily: "'Syne',sans-serif", fontWeight: 700 }}>
                    {activeField?.id === field.id ? "▲ Show less" : "▼ Explore details"}
                  </div>
                </div>
              ))}
            </div>
          </FadeIn>
        )}

        {/* ── NEET Tab ── */}
        {activeTab === "neet" && (
          <FadeIn>
            <div>
              <h2 style={{ fontFamily: "'Playfair Display',serif", fontWeight: 900, fontSize: "clamp(1.4rem,3vw,2rem)", color: dark ? "#f1f5f9" : "#0f172a", margin: "0 0 12px" }}>
                📝 NEET Preparation Roadmap
              </h2>
              <p style={{ fontFamily: "'Lora',serif", color: dark ? "#64748b" : "#94a3b8", marginBottom: 36, fontSize: 14 }}>
                A month-by-month preparation timeline starting from 11th standard.
              </p>

              <div style={{ display: "flex", flexDirection: "column", gap: 12, marginBottom: 48 }}>
                {NEET_TIMELINE.map((item, i) => (
                  <div key={i} style={{
                    display: "flex", gap: 20, alignItems: "center",
                    background: dark ? "rgba(255,255,255,0.03)" : "#fff",
                    border: `1px solid ${dark ? "rgba(255,255,255,0.07)" : "rgba(0,0,0,0.07)"}`,
                    borderRadius: 14, padding: "18px 22px",
                  }}>
                    <span style={{ fontSize: 28, flexShrink: 0 }}>{item.icon}</span>
                    <div style={{ flex: 1 }}>
                      <div style={{ fontFamily: "'Syne',sans-serif", fontWeight: 700, fontSize: 13, color: "#ef4444", marginBottom: 4 }}>{item.month}</div>
                      <div style={{ fontFamily: "'Lora',serif", fontSize: 14, color: dark ? "#cbd5e1" : "#334155", lineHeight: 1.5 }}>{item.task}</div>
                    </div>
                    <div style={{
                      background: "linear-gradient(135deg,#ef4444,#f472b8)",
                      color: "#fff", width: 28, height: 28, borderRadius: "50%",
                      display: "flex", alignItems: "center", justifyContent: "center",
                      fontSize: 12, fontFamily: "'Syne',sans-serif", fontWeight: 700, flexShrink: 0,
                    }}>
                      {i + 1}
                    </div>
                  </div>
                ))}
              </div>

              {/* NEET Subject Breakdown */}
              <div style={{
                background: dark ? "rgba(239,68,68,0.06)" : "rgba(239,68,68,0.04)",
                border: "1px solid rgba(239,68,68,0.2)",
                borderRadius: 20, padding: "32px", marginBottom: 32,
              }}>
                <h3 style={{ fontFamily: "'Playfair Display',serif", fontWeight: 900, fontSize: "clamp(1.2rem,2.5vw,1.6rem)", color: dark ? "#f1f5f9" : "#0f172a", margin: "0 0 20px" }}>
                  📊 NEET Exam Pattern
                </h3>
                <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(200px,1fr))", gap: 16 }}>
                  {[
                    { subject: "Biology", questions: "100 questions", marks: "360 marks", color: "#34d399", tip: "Highest weightage — most important!" },
                    { subject: "Physics", questions: "50 questions", marks: "180 marks", color: "#38bdf8", tip: "Conceptual + numerical both needed" },
                    { subject: "Chemistry", questions: "50 questions", marks: "180 marks", color: "#f59e0b", tip: "Organic Chem is high scoring" },
                  ].map(s => (
                    <div key={s.subject} style={{
                      background: dark ? "rgba(255,255,255,0.04)" : "#fff",
                      borderRadius: 14, padding: "20px",
                      borderTop: `3px solid ${s.color}`,
                      border: `1px solid ${dark ? "rgba(255,255,255,0.06)" : "rgba(0,0,0,0.05)"}`,
                      borderTop: `3px solid ${s.color}`,
                    }}>
                      <div style={{ fontFamily: "'Syne',sans-serif", fontWeight: 800, fontSize: 16, color: s.color, marginBottom: 8 }}>{s.subject}</div>
                      <div style={{ fontFamily: "'Lora',serif", fontSize: 13, color: dark ? "#e2e8f0" : "#1e293b", marginBottom: 4 }}>{s.questions}</div>
                      <div style={{ fontFamily: "'Syne',sans-serif", fontWeight: 700, fontSize: 13, color: dark ? "#94a3b8" : "#64748b", marginBottom: 10 }}>{s.marks}</div>
                      <div style={{ fontFamily: "'Lora',serif", fontSize: 12, color: s.color, fontStyle: "italic" }}>{s.tip}</div>
                    </div>
                  ))}
                </div>
                <div style={{ marginTop: 16, background: dark ? "rgba(255,255,255,0.04)" : "#f8fafc", borderRadius: 12, padding: "14px 18px" }}>
                  <p style={{ fontFamily: "'Syne',sans-serif", fontWeight: 700, fontSize: 13, color: dark ? "#f1f5f9" : "#0f172a", margin: "0 0 4px" }}>Total: 200 questions · 720 marks · 3 hours 20 minutes</p>
                  <p style={{ fontFamily: "'Lora',serif", fontSize: 12, color: dark ? "#64748b" : "#94a3b8", margin: 0 }}>+4 for correct answer · -1 for wrong answer (negative marking)</p>
                </div>
              </div>

              {/* Study Tips */}
              <div style={{
                background: dark ? "rgba(244,114,182,0.06)" : "rgba(244,114,182,0.04)",
                border: "1px solid rgba(244,114,182,0.2)",
                borderRadius: 20, padding: "32px",
              }}>
                <h3 style={{ fontFamily: "'Playfair Display',serif", fontWeight: 900, fontSize: "clamp(1.2rem,2.5vw,1.6rem)", color: dark ? "#f1f5f9" : "#0f172a", margin: "0 0 20px" }}>
                  💡 NEET Study Strategy
                </h3>
                <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(220px,1fr))", gap: 14 }}>
                  {[
                    { tip: "NCERT is everything", detail: "70%+ of NEET questions are directly from NCERT. Read every line, every diagram, every footnote." },
                    { tip: "Biology first, always", detail: "Biology = 360 marks. Master it completely before spending extra time on Physics/Chemistry." },
                    { tip: "Past papers are gold", detail: "Solve last 10 years of NEET papers. You'll see patterns in question types." },
                    { tip: "Diagrams by heart", detail: "Draw the heart, neuron, kidney, cell organelles daily. NEET loves diagram-based questions." },
                    { tip: "Avoid negative marking", detail: "Only attempt if 80%+ sure. Wrong answers cost you marks — be strategic." },
                    { tip: "Mock tests weekly", detail: "Simulate full 3h20min NEET environment. Builds stamina and time management." },
                  ].map(t => (
                    <div key={t.tip} style={{
                      background: dark ? "rgba(255,255,255,0.04)" : "#fff",
                      borderRadius: 12, padding: "16px",
                      border: `1px solid ${dark ? "rgba(255,255,255,0.06)" : "rgba(0,0,0,0.05)"}`,
                    }}>
                      <div style={{ fontFamily: "'Syne',sans-serif", fontWeight: 700, fontSize: 13, color: "#f472b8", marginBottom: 6 }}>✓ {t.tip}</div>
                      <div style={{ fontFamily: "'Lora',serif", fontSize: 12, color: dark ? "#64748b" : "#94a3b8", lineHeight: 1.6 }}>{t.detail}</div>
                    </div>
                  ))}
                </div>
              </div>
            </div>
          </FadeIn>
        )}

        {/* ── Resources Tab ── */}
        {activeTab === "resources" && (
          <FadeIn>
            <h2 style={{ fontFamily: "'Playfair Display',serif", fontWeight: 900, fontSize: "clamp(1.4rem,3vw,2rem)", color: dark ? "#f1f5f9" : "#0f172a", margin: "0 0 32px" }}>
              📚 Best Resources for Medical Aspirants
            </h2>
            <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(240px,1fr))", gap: 20, marginBottom: 48 }}>
              {RESOURCES.map(r => (
                <div key={r.category} style={{
                  background: dark ? "rgba(255,255,255,0.03)" : "#fff",
                  border: `1.5px solid ${dark ? "rgba(255,255,255,0.08)" : "rgba(0,0,0,0.07)"}`,
                  borderRadius: 18, padding: "24px 20px",
                }}>
                  <h3 style={{ fontFamily: "'Syne',sans-serif", fontWeight: 800, fontSize: 15, color: dark ? "#f1f5f9" : "#0f172a", margin: "0 0 16px" }}>{r.category}</h3>
                  {r.items.map(item => (
                    <div key={item} style={{
                      fontFamily: "'Lora',serif", fontSize: 13,
                      color: dark ? "#94a3b8" : "#64748b",
                      padding: "8px 0",
                      borderBottom: `1px solid ${dark ? "rgba(255,255,255,0.05)" : "rgba(0,0,0,0.05)"}`,
                      lineHeight: 1.5,
                    }}>
                      → {item}
                    </div>
                  ))}
                </div>
              ))}
            </div>

            {/* Scholarships */}
            <div style={{
              background: dark ? "rgba(52,211,153,0.07)" : "rgba(52,211,153,0.05)",
              border: "1px solid rgba(52,211,153,0.2)",
              borderRadius: 20, padding: "32px",
            }}>
              <h3 style={{ fontFamily: "'Playfair Display',serif", fontWeight: 900, fontSize: "clamp(1.2rem,2.5vw,1.6rem)", color: dark ? "#f1f5f9" : "#0f172a", margin: "0 0 20px" }}>
                🏆 Scholarships for Medical Students
              </h3>
              <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(220px,1fr))", gap: 12 }}>
                {[
                  { name: "NSP Scholarship", desc: "Central Govt — SC/ST/OBC/Minority students", url: "https://scholarships.gov.in/" },
                  { name: "SSP Karnataka", desc: "State Govt — Post-matric medical scholarship", url: "https://ssp.karnataka.gov.in/ssp_post_home/" },
                  { name: "Vidyadhan", desc: "Merit-cum-need for medical students", url: "https://www.vidyadhan.org/" },
                  { name: "AICTE Saksham", desc: "For differently-abled students in medical", url: "https://www.aicte-india.org/" },
                ].map(s => (
                  <a key={s.name} href={s.url} target="_blank" rel="noopener noreferrer" style={{ textDecoration: "none" }}>
                    <div
                      style={{
                        background: dark ? "rgba(255,255,255,0.04)" : "#fff",
                        borderRadius: 12, padding: "16px",
                        border: "1px solid rgba(52,211,153,0.15)",
                        cursor: "pointer", transition: "all 0.25s",
                      }}
                      onMouseEnter={e => { e.currentTarget.style.borderColor = "#34d399"; e.currentTarget.style.transform = "translateY(-2px)"; }}
                      onMouseLeave={e => { e.currentTarget.style.borderColor = "rgba(52,211,153,0.15)"; e.currentTarget.style.transform = ""; }}
                    >
                      <div style={{ fontFamily: "'Syne',sans-serif", fontWeight: 700, fontSize: 13, color: "#34d399", marginBottom: 4 }}>{s.name} ↗</div>
                      <div style={{ fontFamily: "'Lora',serif", fontSize: 12, color: dark ? "#64748b" : "#94a3b8" }}>{s.desc}</div>
                    </div>
                  </a>
                ))}
              </div>
            </div>
          </FadeIn>
        )}

        {/* ── Wellbeing Tab ── */}
        {activeTab === "wellbeing" && (
          <FadeIn>
            <div>
              <h2 style={{ fontFamily: "'Playfair Display',serif", fontWeight: 900, fontSize: "clamp(1.4rem,3vw,2rem)", color: dark ? "#f1f5f9" : "#0f172a", margin: "0 0 12px" }}>
                🧘 Mental Health & Discipline
              </h2>
              <p style={{ fontFamily: "'Lora',serif", color: dark ? "#64748b" : "#94a3b8", marginBottom: 36, fontSize: 14, lineHeight: 1.8 }}>
                NEET preparation is a marathon, not a sprint. Your mind is your most important asset — protect it.
              </p>

              <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(260px,1fr))", gap: 16, marginBottom: 48 }}>
                {MENTAL_HEALTH_TIPS.map(t => (
                  <div key={t.tip} style={{
                    background: dark ? "rgba(255,255,255,0.03)" : "#fff",
                    border: `1.5px solid ${dark ? "rgba(255,255,255,0.08)" : "rgba(0,0,0,0.07)"}`,
                    borderRadius: 16, padding: "22px 20px",
                    display: "flex", gap: 16, alignItems: "flex-start",
                  }}>
                    <span style={{ fontSize: 32, flexShrink: 0 }}>{t.icon}</span>
                    <div>
                      <div style={{ fontFamily: "'Syne',sans-serif", fontWeight: 700, fontSize: 14, color: dark ? "#f1f5f9" : "#0f172a", marginBottom: 6 }}>{t.tip}</div>
                      <div style={{ fontFamily: "'Lora',serif", fontSize: 13, color: dark ? "#64748b" : "#94a3b8", lineHeight: 1.6 }}>{t.detail}</div>
                    </div>
                  </div>
                ))}
              </div>

              {/* Consistency tracker */}
              <div style={{
                background: dark ? "rgba(129,140,248,0.07)" : "rgba(129,140,248,0.05)",
                border: "1px solid rgba(129,140,248,0.2)",
                borderRadius: 20, padding: "32px",
              }}>
                <h3 style={{ fontFamily: "'Playfair Display',serif", fontWeight: 900, fontSize: "clamp(1.2rem,2.5vw,1.6rem)", color: dark ? "#f1f5f9" : "#0f172a", margin: "0 0 8px" }}>
                  🎯 The Power of Consistency
                </h3>
                <p style={{ fontFamily: "'Lora',serif", color: dark ? "#64748b" : "#94a3b8", fontSize: 14, margin: "0 0 20px", lineHeight: 1.7 }}>
                  If you study just 6 hours daily for 2 years, that's 4,380 hours of focused preparation. No one who puts in that kind of effort fails.
                </p>
                <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(160px,1fr))", gap: 12 }}>
                  {[
                    { label: "Daily study", value: "6–8 hrs" },
                    { label: "Weekly mock tests", value: "2–3" },
                    { label: "Chapters revised/week", value: "5–7" },
                    { label: "NEET past papers", value: "10 years" },
                  ].map(s => (
                    <div key={s.label} style={{
                      background: dark ? "rgba(255,255,255,0.05)" : "#fff",
                      borderRadius: 12, padding: "16px",
                      textAlign: "center",
                      border: `1px solid ${dark ? "rgba(255,255,255,0.08)" : "rgba(0,0,0,0.06)"}`,
                    }}>
                      <div style={{ fontFamily: "'Playfair Display',serif", fontWeight: 900, fontSize: 22, color: "#818cf8", marginBottom: 4 }}>{s.value}</div>
                      <div style={{ fontFamily: "'Syne',sans-serif", fontSize: 11, color: dark ? "#64748b" : "#94a3b8", fontWeight: 600, textTransform: "uppercase", letterSpacing: "0.06em" }}>{s.label}</div>
                    </div>
                  ))}
                </div>
              </div>
            </div>
          </FadeIn>
        )}

        {/* ── Inspiration Tab ── */}
        {activeTab === "stories" && (
          <FadeIn>
            <h2 style={{ fontFamily: "'Playfair Display',serif", fontWeight: 900, fontSize: "clamp(1.4rem,3vw,2rem)", color: dark ? "#f1f5f9" : "#0f172a", margin: "0 0 32px" }}>
              🌟 Doctors Who Inspired the World
            </h2>
            <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(280px,1fr))", gap: 24, marginBottom: 48 }}>
              {STORIES.map(s => (
                <div key={s.name} style={{
                  background: dark ? "rgba(255,255,255,0.03)" : "#fff",
                  border: `1.5px solid ${dark ? "rgba(255,255,255,0.08)" : "rgba(0,0,0,0.07)"}`,
                  borderRadius: 20, padding: "28px 24px",
                  position: "relative", overflow: "hidden",
                }}>
                  <div style={{ position: "absolute", top: 0, right: 0, width: 80, height: 80, background: "linear-gradient(135deg,#ef444411,#f472b811)", borderRadius: "0 20px 0 80px" }} />
                  <div style={{ fontSize: 48, marginBottom: 16 }}>👨‍⚕️</div>
                  <h3 style={{ fontFamily: "'Playfair Display',serif", fontWeight: 900, fontSize: 20, color: dark ? "#f1f5f9" : "#0f172a", margin: "0 0 4px" }}>{s.name}</h3>
                  <p style={{ fontFamily: "'Syne',sans-serif", fontWeight: 600, fontSize: 12, color: "#ef4444", margin: "0 0 16px" }}>{s.role}</p>
                  <div style={{ background: dark ? "rgba(255,255,255,0.05)" : "#f8fafc", borderRadius: 12, padding: "14px 16px", borderLeft: "3px solid #f472b8" }}>
                    <p style={{ fontFamily: "'Lora',serif", fontStyle: "italic", fontSize: 13, color: dark ? "#cbd5e1" : "#334155", margin: 0, lineHeight: 1.7 }}>
                      "{s.quote}"
                    </p>
                  </div>
                  <span style={{ display: "inline-block", marginTop: 12, background: "#ef444418", color: "#ef4444", border: "1px solid #ef444433", padding: "3px 12px", borderRadius: 20, fontSize: 11, fontFamily: "'Syne',sans-serif", fontWeight: 700 }}>
                    {s.field}
                  </span>
                </div>
              ))}
            </div>

            {/* Motivational closing */}
            <div style={{
              background: dark ? "linear-gradient(135deg,rgba(239,68,68,0.08),rgba(244,114,182,0.08))" : "linear-gradient(135deg,rgba(239,68,68,0.05),rgba(244,114,182,0.04))",
              border: "1px solid rgba(239,68,68,0.15)",
              borderRadius: 20, padding: "32px", textAlign: "center",
            }}>
              <p style={{ fontSize: 36, marginBottom: 12 }}>🩺</p>
              <h3 style={{ fontFamily: "'Playfair Display',serif", fontWeight: 900, fontSize: "clamp(1.2rem,2.5vw,1.7rem)", color: dark ? "#f1f5f9" : "#0f172a", margin: "0 0 12px", lineHeight: 1.4 }}>
                "The good physician treats the disease. The great physician treats the patient."
              </h3>
              <p style={{ fontFamily: "'Syne',sans-serif", fontWeight: 700, fontSize: 12, color: "#ef4444", letterSpacing: "0.1em", textTransform: "uppercase" }}>— William Osler</p>
            </div>
          </FadeIn>
        )}

        {/* ── CTA ── */}
        <FadeIn>
          <div style={{
            textAlign: "center", marginTop: 72,
            padding: "48px 32px",
            background: dark ? "linear-gradient(135deg,rgba(239,68,68,0.07),rgba(244,114,182,0.07))" : "linear-gradient(135deg,rgba(239,68,68,0.05),rgba(244,114,182,0.04))",
            borderRadius: 28,
            border: `1px solid ${dark ? "rgba(255,255,255,0.06)" : "rgba(0,0,0,0.05)"}`,
          }}>
            <p style={{ fontSize: 44, marginBottom: 16 }}>🩺</p>
            <h2 style={{ fontFamily: "'Playfair Display',serif", fontWeight: 900, fontSize: "clamp(1.4rem,3vw,2.2rem)", color: dark ? "#f1f5f9" : "#0f172a", margin: "0 0 14px", lineHeight: 1.4 }}>
              Every doctor was once a student who refused to give up.
            </h2>
            <p style={{ fontFamily: "'Lora',serif", color: dark ? "#64748b" : "#94a3b8", fontSize: 15, maxWidth: 480, margin: "0 auto 28px", lineHeight: 1.8 }}>
              NEET is hard. Medicine is harder. But saving a life? Nothing compares.
            </p>
            <div style={{ display: "flex", gap: 12, justifyContent: "center", flexWrap: "wrap" }}>
              <a href="#community" style={{ background: "linear-gradient(135deg,#ef4444,#f472b8)", color: "#fff", padding: "12px 28px", borderRadius: 14, fontFamily: "'Syne',sans-serif", fontWeight: 700, fontSize: 14, textDecoration: "none" }}>
                Ask a Medical Senior →
              </a>
              <a href="#scholarships" style={{ background: dark ? "rgba(255,255,255,0.07)" : "#f1f5f9", color: dark ? "#f1f5f9" : "#0f172a", padding: "12px 28px", borderRadius: 14, fontFamily: "'Syne',sans-serif", fontWeight: 700, fontSize: 14, textDecoration: "none", border: `1px solid ${dark ? "rgba(255,255,255,0.1)" : "rgba(0,0,0,0.08)"}` }}>
                Find Scholarships
              </a>
            </div>
          </div>
        </FadeIn>

      </div>
    </section>
  );
}
